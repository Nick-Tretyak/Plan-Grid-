import { columnsStyle } from "../ColumnsStyle"

const Backlog = () => {
    return (
        <div css={columnsStyle} draggable={true}>
            Backlog
        </div>
    )
}

export default Backlog